{
  "name": "npu-ng",
  "version": "0.10.0",
  "description": "Neptun PowerUp! NG – felturbózza az új Neptun webes felületét",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:site": "npm run build && cp dist/npu.user.js site/npu.user.js",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.127.1"
  }
}
