{
  "name": "npu-ng",
  "version": "0.12.1",
  "description": "Neptun PowerUp! NG – felturbózza az új Neptun webes felületét",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:site": "npm run build && cp dist/npu.user.js site/npu.user.js && node -e \"const p=require('./package.json');require('fs').writeFileSync('site/version.json',JSON.stringify({version:p.version})+'\\n')\"",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node test/run.mjs"
  },
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.127.1"
  }
}
